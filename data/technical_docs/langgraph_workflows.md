LangGraph workflow notes.

Use graphs when the interview system needs state, routing, interrupts, retries, or tool selection across multiple steps.
Strong explanations mention planner, retriever, interviewer, evaluator, and persistence boundaries.
Candidates should discuss when a simple chain is enough and when graph orchestration adds real value.
